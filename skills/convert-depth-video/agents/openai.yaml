interface:
  display_name: "Depth Video Converter"
  short_description: "Convert MP4/MOV files into local grayscale depth videos"
  default_prompt: "Use $convert-depth-video to launch the local depth video converter and help me process a video."

policy:
  allow_implicit_invocation: true
